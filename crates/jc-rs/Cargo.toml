[package]
name = "jc-rs"
version.workspace = true
edition.workspace = true
description = "jc-rs: convert CLI output, file types, and strings to JSON/YAML (Rust rewrite of jc)"
readme = "README.md"
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords = ["json", "cli", "parser", "jc", "yaml"]
categories = ["command-line-utilities", "encoding"]

[[bin]]
name = "jc-rs"
path = "src/main.rs"

# `cargo binstall jc-rs` fetches the release archive instead of building from
# source. The paths mirror what release.yml actually produces: the tag carries
# a `v`, and the binary sits inside a directory named after the archive.
[package.metadata.binstall]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-v{ version }-{ target }.{ archive-format }"
bin-dir = "{ name }-v{ version }-{ target }/{ bin }{ binary-ext }"
pkg-fmt = "tgz"

[package.metadata.binstall.overrides.x86_64-pc-windows-msvc]
pkg-fmt = "zip"

[dependencies]
jc-rs-core = { path = "../jc-rs-core", version = "0.5.1" }
jc-rs-parsers = { path = "../jc-rs-parsers", version = "0.5.1" }
jc-rs-utils = { path = "../jc-rs-utils", version = "0.5.1" }
serde_json = { workspace = true }
serde-saphyr = "1"
colored = "3"

# musl only: measured, not assumed. On glibc mimalloc is a wash on big
# inputs and 22% slower on small ones; on musl it is worth up to 2.2x.
[target.'cfg(target_env = "musl")'.dependencies]
mimalloc = { version = "0.1", default-features = false }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
